import glupredkit.api as gpk


# Parse data and get data (if parsed data avail, use that, else, use parser + save!)
#data = gpk.get_synthetic_data()
#data = gpk.get_parsed_data('OhioT1DM.csv')
# TODO HERE: if a given file name available, get it, else, parse data
data = gpk.parse_data()








# TODO: Should split it into train model(s), test model(s), and draw plots!





